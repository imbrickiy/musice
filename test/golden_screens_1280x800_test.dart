import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets(
    'Golden 1280x800 placeholder (skipped until golden baseline is added)',
    (tester) async {
      expect(true, isTrue);
    },
    skip: true,
  );
}
